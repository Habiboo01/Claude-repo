<div style="padding: calc(var(--header-height) + var(--spacing-xl)) var(--gutter) var(--spacing-xl); max-width: 900px; margin: 0 auto;">
  <h1 class="heading-lg reveal" style="margin-bottom:var(--spacing-lg);">{{ page.title }}</h1>
  <div class="rte reveal" style="font-size:16px; line-height:1.9; color:var(--color-muted);">
    {{ page.content }}
  </div>
</div>

{% schema %}
{ "name": "Page", "tag": "section", "settings": [] }
{% endschema %}
